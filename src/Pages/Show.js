import TransactionDetails from '../Components/TransactionDetails';

const Show = () => {
  return (
    <section>
      <h2>Trasaction Details</h2>
      <TransactionDetails />
    </section>
  );
};

export default Show;
