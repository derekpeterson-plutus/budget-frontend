import TransactionNewForm from '../Components/TransactionNewForm';

const New = () => {
  return (
    <section>
      <h2>Add new transaction</h2>
      <TransactionNewForm />
    </section>
  );
};

export default New;
